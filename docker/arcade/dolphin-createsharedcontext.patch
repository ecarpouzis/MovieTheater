diff --git a/Source/Core/Common/CMakeLists.txt b/Source/Core/Common/CMakeLists.txt
index 0ed507c..a3d02ba 100644
--- a/Source/Core/Common/CMakeLists.txt
+++ b/Source/Core/Common/CMakeLists.txt
@@ -325,12 +325,15 @@ if(WIN32)
     WindowsDevice.cpp
     WindowsDevice.h
   )
-  if(NOT LIBRETRO)
-    target_sources(common PRIVATE
-      GL/GLInterface/WGL.cpp
-      GL/GLInterface/WGL.h
-    )
-  endif()
+  # MovieTheater: WGL.cpp is needed under LIBRETRO too â€” GLContextLR mints REAL share-linked
+  # worker contexts from it (CreateExternalSharedContext), which is what makes async shader
+  # compilation possible against a frontend-owned GL context.
+  target_sources(common PRIVATE
+    GL/GLInterface/WGL.cpp
+    GL/GLInterface/WGL.h
+  )
+  # wgl* imports for the WGL context code above.
+  target_link_libraries(common PRIVATE opengl32.lib)
   if(MSVC)
     target_sources(common PRIVATE
       CompatPatches.cpp
diff --git a/Source/Core/Common/GL/GLInterface/WGL.cpp b/Source/Core/Common/GL/GLInterface/WGL.cpp
index a85a27a..26cc8ea 100644
--- a/Source/Core/Common/GL/GLInterface/WGL.cpp
+++ b/Source/Core/Common/GL/GLInterface/WGL.cpp
@@ -368,6 +368,68 @@ std::unique_ptr<GLContext> GLContextWGL::CreateSharedContext()
   return context;
 }
 
+static HDC s_external_main_dc = nullptr;
+static HGLRC s_external_main_rc = nullptr;
+
+bool GLContextWGL::CaptureExternalMainContext()
+{
+  s_external_main_dc = wglGetCurrentDC();
+  s_external_main_rc = wglGetCurrentContext();
+  if (!s_external_main_dc || !s_external_main_rc)
+  {
+    WARN_LOG_FMT(VIDEO, "CaptureExternalMainContext: no current WGL context on this thread");
+    return false;
+  }
+  LoadWGLExtensions();  // needs a current context, which we verifiably have right now
+  if (!wglCreateContextAttribsARB || !wglCreatePbufferARB || !wglGetPbufferDCARB)
+  {
+    WARN_LOG_FMT(VIDEO, "CaptureExternalMainContext: WGL extensions unavailable");
+    return false;
+  }
+  INFO_LOG_FMT(VIDEO, "CaptureExternalMainContext: captured frontend dc/rc, extensions loaded");
+  return true;
+}
+
+std::unique_ptr<GLContext> GLContextWGL::CreateExternalSharedContext()
+{
+  if (!s_external_main_dc || !s_external_main_rc)
+    return nullptr;
+
+  HANDLE pbuffer;
+  HDC dc;
+  if (!CreatePBuffer(s_external_main_dc, 1, 1, &pbuffer, &dc))
+    return nullptr;
+
+  HGLRC rc = CreateCoreContext(dc, s_external_main_rc);
+  if (!rc)
+  {
+    // Core-profile share against the frontend's legacy/compat context can be refused by some
+    // drivers; fall back to a plain compat context + wglShareLists â€” legal here because the new
+    // context is brand-new and empty.
+    rc = wglCreateContext(dc);
+    if (rc && !wglShareLists(s_external_main_rc, rc))
+    {
+      WARN_LOG_FMT(VIDEO, "CreateExternalSharedContext: wglShareLists failed ({})", GetLastError());
+      wglDeleteContext(rc);
+      rc = nullptr;
+    }
+  }
+  if (!rc)
+  {
+    wglReleasePbufferDCARB(static_cast<HPBUFFERARB>(pbuffer), dc);
+    wglDestroyPbufferARB(static_cast<HPBUFFERARB>(pbuffer));
+    return nullptr;
+  }
+
+  std::unique_ptr<GLContextWGL> context = std::make_unique<GLContextWGL>();
+  context->m_pbuffer_handle = pbuffer;
+  context->m_dc = dc;
+  context->m_rc = rc;
+  context->m_opengl_mode = Mode::OpenGL;
+  context->m_is_shared = true;
+  return context;
+}
+
 HGLRC GLContextWGL::CreateCoreContext(HDC dc, HGLRC share_context)
 {
   if (!wglCreateContextAttribsARB)
diff --git a/Source/Core/Common/GL/GLInterface/WGL.h b/Source/Core/Common/GL/GLInterface/WGL.h
index 2f5a81e..4322af8 100644
--- a/Source/Core/Common/GL/GLInterface/WGL.h
+++ b/Source/Core/Common/GL/GLInterface/WGL.h
@@ -26,6 +26,14 @@ public:
 
   void* GetFuncAddress(const std::string& name) override;
 
+  // MovieTheater (libretro external-context support): the FRONTEND owns the main GL context, but
+  // Dolphin's shader-compiler threads still need real share-linked contexts. Capture the frontend's
+  // dc/rc (and load the WGL extension pointers) while its context is CURRENT â€” GLContextLR calls
+  // this from Initialize, which runs inside context_reset on the frontend's GL thread â€” then mint
+  // pbuffer-backed shared contexts from any thread.
+  static bool CaptureExternalMainContext();
+  static std::unique_ptr<GLContext> CreateExternalSharedContext();
+
 protected:
   bool Initialize(const WindowSystemInfo& wsi, bool stereo, bool core) override;
 
diff --git a/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.cpp b/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.cpp
index 1a42fb7..28c8f54 100644
--- a/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.cpp
+++ b/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.cpp
@@ -6,6 +6,9 @@
 #include "DolphinLibretro/Common/Options.h"
 #include "DolphinLibretro/VideoContexts/ContextStatus.h"
 #include "DolphinLibretro/Video.h"
+#ifdef _WIN32
+#include "Common/GL/GLInterface/WGL.h"
+#endif
 
 GLContextLR::GLContextLR()
 {
@@ -45,9 +48,25 @@ bool GLContextLR::Initialize(const WindowSystemInfo& wsi, bool stereo, bool core
 
   m_initialized = true;
 
+#ifdef _WIN32
+  // The frontend's GL context is current on THIS thread right now (we are inside context_reset â†’
+  // backend init). Capture it so CreateSharedContext can mint share-linked worker contexts later.
+  if (m_opengl_mode == Mode::OpenGL)
+    GLContextWGL::CaptureExternalMainContext();
+#endif
+
   return true;
 }
 
+std::unique_ptr<GLContext> GLContextLR::CreateSharedContext()
+{
+#ifdef _WIN32
+  if (m_opengl_mode == Mode::OpenGL)
+    return GLContextWGL::CreateExternalSharedContext();
+#endif
+  return nullptr;
+}
+
 void GLContextLR::Swap()
 {
   Libretro::Video::video_cb(VideoCommon::g_is_duplicate_frame ? nullptr : RETRO_HW_FRAME_BUFFER_VALID,
diff --git a/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.h b/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.h
index 84238cb..38086c9 100644
--- a/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.h
+++ b/Source/Core/DolphinLibretro/VideoContexts/GLContextLR.h
@@ -11,6 +11,9 @@ public:
   ~GLContextLR() override;
   
   bool IsHeadless() const override { return false; }
+  // MovieTheater: real share-linked worker contexts (async shader compilation). The base class
+  // returns nullptr, which Dolphin's async paths dereference â€” the frontend-side crash we chased.
+  std::unique_ptr<GLContext> CreateSharedContext() override;
   void Swap() override;
   void* GetFuncAddress(const std::string& name) override;
   bool Initialize(const WindowSystemInfo& wsi, bool stereo, bool core) override;
