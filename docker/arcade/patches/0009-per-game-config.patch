--- a/pkg/config/emulator.go
+++ b/pkg/config/emulator.go
@@ -83,6 +83,7 @@
 	NonBlockingSave bool
 	Options         map[string]string
 	Options4rom     map[string]map[string]string // <(^_^)>
+	Fps4rom         map[string]float64            // per-ROM forced framerate override (arcade per-game config)
 	Roms            []string
 	SaveStateFs     string
 	Scale           float64
--- a/pkg/worker/caged/libretro/frontend.go
+++ b/pkg/worker/caged/libretro/frontend.go
@@ -177,6 +177,7 @@
 		LibPath:         conf.Lib,
 		Options:         conf.Options,
 		Options4rom:     conf.Options4rom,
+		Fps4rom:         conf.Fps4rom,
 		UsesLibCo:       conf.UsesLibCo,
 		CoreAspectRatio: conf.CoreAspectRatio,
 		KbMouseSupport:  conf.KbMouseSupport,
--- a/pkg/worker/caged/libretro/nanoarch/nanoarch.go
+++ b/pkg/worker/caged/libretro/nanoarch/nanoarch.go
@@ -47,6 +47,7 @@
 	meta          Metadata
 	options       map[string]string
 	options4rom   map[string]map[string]string
+	fps4rom       map[string]float64
 	reserved      chan struct{} // limits concurrent use
 	Rot           uint
 	serializeSize C.size_t
@@ -103,6 +104,7 @@
 	HasVFR          bool
 	Options         map[string]string
 	Options4rom     map[string]map[string]string
+	Fps4rom         map[string]float64
 	Hacks           []string
 	Hid             map[int][]int
 	CoreAspectRatio bool
@@ -209,6 +211,7 @@
 
 	n.options = maps.Clone(meta.Options)
 	n.options4rom = meta.Options4rom
+	n.fps4rom = meta.Fps4rom
 
 	corePath := meta.LibPath + meta.LibExt
 	coreLib, err = open(corePath)
@@ -286,9 +289,10 @@
 
 	n.log.Debug().Msgf("ROM - big: %v, size: %v", big, byteCountBinary(int64(game.size)))
 
-	// maybe some custom options
+	romName := strings.TrimSuffix(filepath.Base(path), filepath.Ext(path))
+
+	// maybe some custom options (static per-ROM overrides from config.yaml)
 	if n.options4rom != nil {
-		romName := strings.TrimSuffix(filepath.Base(path), filepath.Ext(path))
 		if _, ok := n.options4rom[romName]; ok {
 			for k, v := range n.options4rom[romName] {
 				n.options[k] = v
@@ -297,6 +301,16 @@
 		}
 	}
 
+	// Arcade per-game config manifest (game-overrides.json, DB-driven via arcade-gameconfig-export).
+	// Core-option overrides here win over config; the forced-fps field is applied after av-info below.
+	override, hasOverride := gameOverrides()[romName]
+	if hasOverride {
+		for k, v := range override.Options {
+			n.options[k] = v
+			n.log.Info().Msgf("[game-override] option %v=%v for %q", k, v, romName)
+		}
+	}
+
 	if ok := C.bridge_retro_load_game(retroLoadGame, &game); !ok {
 		return fmt.Errorf("core failed to load ROM: %v", path)
 	}
@@ -313,6 +327,23 @@
 	}
 	n.sys.av = av
 
+	// Per-ROM forced framerate (arcade per-game config). Some cores advertise the DISPLAY refresh
+	// (e.g. flycast reports 59.94 for Dreamcast) while the GAME's engine is hardcoded to a lower rate
+	// with physics tied to it — Sonic Adventure is locked to 30fps, so pacing retro_run at 59.94Hz runs
+	// it at DOUBLE SPEED. Overriding the stored timing here makes tickTime, VideoFramerate() and the
+	// frontend pacing loop all target the game's true rate. Only listed games are affected; genuine
+	// 60fps titles are untouched (no global slowdown). Source: manifest wins, config fps4rom is fallback.
+	forcedFps := 0.0
+	if hasOverride && override.Fps > 0 {
+		forcedFps = override.Fps
+	} else if n.fps4rom != nil {
+		forcedFps = n.fps4rom[romName]
+	}
+	if forcedFps > 0 {
+		n.log.Info().Msgf("[game-override] forcing %.3f fps for %q (core advertised %.3f)", forcedFps, romName, float64(n.sys.av.timing.fps))
+		n.sys.av.timing.fps = C.double(forcedFps)
+	}
+
 	n.serializeSize = C.bridge_retro_serialize_size(retroSerializeSize)
 	n.log.Info().Msgf("Save file size: %v", byteCountBinary(int64(n.serializeSize)))
 
--- /dev/null
+++ b/pkg/worker/caged/libretro/nanoarch/overrides.go
@@ -0,0 +1,65 @@
+package nanoarch
+
+import (
+	"encoding/json"
+	"os"
+	"sync"
+	"time"
+)
+
+// GameOverride is one ROM's entry in the arcade per-game config manifest (game-overrides.json,
+// generated by the site's `arcade-gameconfig-export` from the ArcadeGameProfile table). It is keyed
+// by ROM filename sans extension (== ArcadeGame.CloudRetroGameKey, the same key options4rom uses).
+// See docs/arcade-per-game-config.md.
+//
+//	{
+//	  "Sonic Adventure v1.005 (1999)(Sega)(US)(M5)[!][%51000-A]": { "fps": 30 },
+//	  "Some Game (USA)": { "options": { "flycast_widescreen_hacks": "enabled" } }
+//	}
+type GameOverride struct {
+	Fps     float64           `json:"fps"`     // forced framerate (0 = leave the core's advertised rate)
+	Options map[string]string `json:"options"` // libretro core-option overrides applied before load
+}
+
+var (
+	goMu     sync.Mutex
+	goCache  map[string]GameOverride
+	goMod    time.Time
+	goLoaded bool
+	goPath   string
+)
+
+// gameOverrides returns the current per-game manifest (romName → override), transparently reloading
+// when the file's mtime changes so admin edits take effect on the NEXT room without a worker restart.
+// Path: CLOUD_GAME_GAME_OVERRIDES, else "game-overrides.json" resolved from the worker CWD (the same
+// ConfDir config.yaml loads from). A missing file means "no overrides" (nil); a parse error keeps the
+// last good map so a bad regen can't wipe live behavior mid-session.
+func gameOverrides() map[string]GameOverride {
+	path := os.Getenv("CLOUD_GAME_GAME_OVERRIDES")
+	if path == "" {
+		path = "game-overrides.json"
+	}
+
+	goMu.Lock()
+	defer goMu.Unlock()
+
+	fi, err := os.Stat(path)
+	if err != nil {
+		goCache, goLoaded, goPath = nil, true, path
+		return nil
+	}
+	if goLoaded && path == goPath && fi.ModTime().Equal(goMod) {
+		return goCache
+	}
+
+	data, err := os.ReadFile(path)
+	if err != nil {
+		return goCache
+	}
+	var m map[string]GameOverride
+	if err := json.Unmarshal(data, &m); err != nil {
+		return goCache // keep last-good on malformed JSON
+	}
+	goCache, goMod, goLoaded, goPath = m, fi.ModTime(), true, path
+	return m
+}
