apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: movietheater-tls
  namespace: movietheater
spec:
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  secretName: movietheater-tls
  dnsNames:
  - theater.carpouzis.com
