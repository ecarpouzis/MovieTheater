# Downloaded Commons originals + the resolved credit map. Purely a fetch cache: deleting it costs a
# re-download, nothing else. The tiles it builds ARE committed (src/ui/src/assets/consoles).
.cache/
