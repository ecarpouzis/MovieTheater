{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://localhost:2303"
      }
    }
  },
  "CoordinatorBaseUrl": "http://localhost:8000",
  "ArcadeTokenSecret": null,
  "SiteOrigin": "https://your-movie-site.example",

  "//RomCache": "Just-in-time ROM cache (docs/arcade-jit-cache.md). Leave ManifestPath/RomsDir empty to disable (gateway = pure signaling proxy). ManifestPath = the file arcade-romcache-export writes; RomsDir = the same host path as compose ROMS_DIR (the workers mount it read-only). MaxBytes caps extracted JIT ROMs on disk.",
  "RomCache": {
    "ManifestPath": "",
    "RomsDir": "",
    "MaxBytes": 32212254720,
    "SevenZipPath": "C:\\Program Files\\7-Zip\\7z.exe",
    "MaxParallelExtractions": 1,
    "ExtractTimeoutSeconds": 300
  }
}
