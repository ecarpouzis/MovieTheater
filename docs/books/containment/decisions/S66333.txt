# X-Factor (1986) (S66333) — 27 collected editions, 376 issue files, 146 numbers naming two files.
#
# X-Factor v1 (1986) #1-149 sits here together with v2, v3 (2006) and v4 (2020), and the v1 issues are
# filed twice — once in \X-Factor\01 X-Factor v1 (1986) and again through the X-Men Complete Chronology
# arc folders. That is where the 146 repeated numbers come from. Five of the "collections" (89522-89525,
# 82706) are 24-29pp X-Factor v2 single issues.
# The claims are the widest on this half of the queue: LOCG's #1-286 on the 203pp Vol. 04 covers all 376
# issue files, #9-373 on the 107pp Vol. 16 covers 330, and #11-374 sits on two different books at once
# (the 448pp Complete Collection Book 02 and the 93pp Vol. 17) covering 320 each.



# the remaining 22 editions: four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds

N 66333 X-Factor v1 (1986), v2, v3 (2006) and v4 (2020) share this Series id with the v1 issues filed twice; 146 numbers name more than one file, and five items flagged as collections are 24-29pp v2 floppies
F 85671 arithmetic-odd | LOCG's #1-286 on the 203pp Vol. 04 covers all 376 issue files on the shelf
F 85683 arithmetic-odd | LOCG's #9-373 on the 107pp Vol. 16 covers 330 issue files
F 85695 duplicate-edition | LOCG's #11-374 sits on both the 448pp Complete Collection Book 02 and the 93pp Vol. 17, covering 320 files each
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85668 now sits on S22153, whose file decides it
# u 85668 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85669 now sits on S22153, whose file decides it
# u 85669 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85670 now sits on S22153, whose file decides it
# u 85670 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85671 now sits on S22153, whose file decides it
# u 85671 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85672 now sits on S22153, whose file decides it
# u 85672 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85673 now sits on S22153, whose file decides it
# u 85673 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85676 now sits on S22153, whose file decides it
# u 85676 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85677 now sits on S22153, whose file decides it
# u 85677 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85678 now sits on S22153, whose file decides it
# u 85678 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85679 now sits on S22153, whose file decides it
# u 85679 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85680 now sits on S22153, whose file decides it
# u 85680 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85681 now sits on S22153, whose file decides it
# u 85681 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85682 now sits on S22153, whose file decides it
# u 85682 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85683 now sits on S22153, whose file decides it
# u 85683 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85684 now sits on S22153, whose file decides it
# u 85684 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85685 now sits on S22153, whose file decides it
# u 85685 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85686 now sits on S22153, whose file decides it
# u 85686 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85687 now sits on S22153, whose file decides it
# u 85687 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85688 now sits on S22153, whose file decides it
# u 85688 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85694 now sits on S104235, whose file decides it
# u 85694 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 85695 now sits on S104235, whose file decides it
# u 85695 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 92972 now sits on S22172, whose file decides it
# u 92972 four X-Factor runs number from #1 on this shelf and 146 numbers name more than one file; no range can say which run's issues a volume holds
