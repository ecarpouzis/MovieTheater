# Queen Bee (S14275) — 1 volumes; 1 state their own chapters.
#
# Every page inside these archives is named with the chapter it belongs to, so each
# volume states its contents exactly. The ranges below are CHAPTER numbers read from
# the archives themselves, and they form a ladder: single volume.
# Volumes whose rip names no chapter are refused: a volume number is not a chapter
# number and must not stand in for one (PLAN.md §4.2).

N 14275 chapter ranges read from each archive's own page names; the ladder tiles (single volume)
# MOVED (series rebuild, retired by retire_moved_lines.py): item 45182 now sits on S104106, whose file decides it
# S 45182 0 3 0.97 | Queen Bee! v01 | the archive names the chapter on every page and this volume holds 4 of them, c0-c3
