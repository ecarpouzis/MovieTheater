# X-Men: Red (S22296) — 2 collected editions, 21 issue files, #2, #5 and #6 doubled.
#
# X-Men Red v1 (2018) and X-Men Red v2 (2022) share this Series id and both number from #1. The two
# books are Al Ewing's, from the 2022 run, and neither carries a claim of any kind — so the only thing
# that could place them is the ordinal, in a numbering the 2018 run shares.



# the remaining 2 editions: no claim on either book and two runs share the numbering; a range would land on the 2018 issues of the same numbers

N 22296 X-Men Red v1 (2018) and v2 (2022) share this Series id with #2, #5 and #6 each naming two files; neither volume carries a provider claim
F 86316 conflated-series | X-Men Red v1 (2018) and v2 (2022) both number from #1 under one Series id
# MOVED (series rebuild, retired by retire_moved_lines.py): item 86316 now sits on S94820, whose file decides it
# u 86316 no claim on either book and two runs share the numbering; a range would land on the 2018 issues of the same numbers
# MOVED (series rebuild, retired by retire_moved_lines.py): item 86317 now sits on S94820, whose file decides it
# u 86317 no claim on either book and two runs share the numbering; a range would land on the 2018 issues of the same numbers

# ADDENDUM (identity pass, wave 4, 2026-09-16): the identity reading attributed the shelf(s) below to the run this shelf IS, and the resolve merged their collected
# editions in. The coverage contract says every collected edition on a shelf is decided exactly once, so they are refused here until read.
u 86314 arrived by the wave-4 identity merge from S94819; it carries 2 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
u 86315 arrived by the wave-4 identity merge from S94819; it carries 2 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
