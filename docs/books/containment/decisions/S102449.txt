# S102449 Green Lantern Corps — created by the identity pass, wave 2, 2026-09-10.
# This shelf had no containment decision file and the identity merge moved 5 collected edition(s) onto it, so the file must now cover ALL 5 of its editions.
# Nothing here is a range. Every line is a refusal until the book is read (containment PLAN §8, §10.3).
u 65453 arrived by the wave-2 identity merge from S7927; no range read from the book
u 65454 arrived by the wave-2 identity merge from S7927; no range read from the book
u 65455 arrived by the wave-2 identity merge from S7927; no range read from the book
u 65456 arrived by the wave-2 identity merge from S7927; no range read from the book
u 65457 arrived by the wave-2 identity merge from S7927; no range read from the book

# ADDENDUM (identity pass, wave 5, 2026-09-16): the identity reading attributed the shelf(s) below to the run this shelf IS, and the resolve merged their collected
# editions in. The coverage contract says every collected edition on a shelf is decided exactly once, so they are refused here until read.
# MOVED (series rebuild, retired by retire_moved_lines.py): item 65458 now sits on S103791, whose file decides it
# u 65458 arrived by the wave-5 identity merge from S94607; it carries 1 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
# MOVED (series rebuild, retired by retire_moved_lines.py): item 65459 now sits on S103791, whose file decides it
# u 65459 arrived by the wave-5 identity merge from S94607; it carries 1 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
# MOVED (series rebuild, retired by retire_moved_lines.py): item 65460 now sits on S103791, whose file decides it
# u 65460 arrived by the wave-5 identity merge from S94607; it carries 2 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
# MOVED (series rebuild, retired by retire_moved_lines.py): item 65461 now sits on S103791, whose file decides it
# u 65461 arrived by the wave-5 identity merge from S94607; no range read from the book
# MOVED (series rebuild, retired by retire_moved_lines.py): item 65462 now sits on S103791, whose file decides it
# u 65462 arrived by the wave-5 identity merge from S94607; it carries 1 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
# MOVED (series rebuild, retired by retire_moved_lines.py): item 65463 now sits on S103791, whose file decides it
# u 65463 arrived by the wave-5 identity merge from S94607; it carries 1 unjudged provider span(s), which the merge would ARM against a run they were never measured against; no range read from the book
