

# MOVED (series rebuild, retired by retire_moved_lines.py): item 96931 now sits on S94813, whose file decides it
# u 96931 three runs number from #1 on this shelf and three collection lines share the ordinals; no range identifies which run's issues a book holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96932 now sits on S94813, whose file decides it
# u 96932 three runs number from #1 on this shelf and three collection lines share the ordinals; no range identifies which run's issues a book holds
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96933 now sits on S94813, whose file decides it
# u 96933 three runs number from #1 on this shelf and three collection lines share the ordinals; no range identifies which run's issues a book holds
