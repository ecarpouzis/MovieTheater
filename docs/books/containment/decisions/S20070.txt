# Thor (S20070) — 7 collected editions, 27 issue files in TWO numberings.
#
# The ladder says it plainly: #12, #13, #14, #17, #30 on one side and #345-#471 on the other. This Series
# id carries the 1966 run in both its original and its legacy numbering, plus the Straczynski (2007) and
# Jason Aaron (2014) relaunches that each start at #1, and the collections come from four different
# lines (Simonson, Straczynski, Jason Aaron's Complete Collection, the Straczynski/Gillen Omnibus).
# Three different books — items 117957, 96839 and 96846 — all carry the SAME LOCG claim, #107-114, which
# is one claim being handed to whichever book matched a title.



# the remaining 7 editions: the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues

N 20070 two numberings on one shelf (#12-#30 and #345-#471) and four collection lines over three runs; items 117957, 96839 and 96846 all carry the identical LOCG claim #107-114
F 96764 conflated-series | the 1966 run (original and legacy numbering), Straczynski's 2007 relaunch and Jason Aaron's 2014 run share this Series id
F 117957 duplicate-edition | items 117957, 96839 and 96846 are three different books all carrying LOCG's #107-114
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96764 now sits on S103613, whose file decides it
# u 96764 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96839 now sits on S94809, whose file decides it
# u 96839 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96846 now sits on S20119, whose file decides it
# u 96846 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96847 now sits on S20119, whose file decides it
# u 96847 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96848 now sits on S20119, whose file decides it
# u 96848 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
u 96850 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 117957 now sits on S94807, whose file decides it
# u 117957 the shelf carries two numberings at once and three runs that each start at #1; no range written in one of them can avoid claiming another's issues
