# Ms. Marvel (S12433) — 16 collected editions, 27 issue files, four numbers naming two files each.
#
# Two runs share this Series id: Carol Danvers' Ms. Marvel (2006), whose issues sit in the Civil War,
# Initiative and Dark Reign chronology folders, and Kamala Khan's Ms. Marvel (2014). Both number from #1,
# and #6, #7, #8 and #13 each name a file from both.
# Two collection LINES then sit over the Kamala run — "Vol. NN" and "by G. Willow Wilson Book NN" — so
# the volume ordinal is not unique either, and four of the sixteen books carry LOCG spans of the
# ordinal shape §6.2 describes (#4-4 on Vol. 03, #9-9 on Vol. 07, #10-10 on Vol. 08), which come from
# LOCG records that are single comics rather than editions.
# The damage is measurable: LOCG's #1-22 sits on three different books and covers 18 of the 27 files.



# the remaining 16 editions: two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues

N 12433 Ms. Marvel (2006) and Ms. Marvel (2014) share this Series id with #6, #7, #8 and #13 naming two files each; two collection lines then reuse the same volume ordinals over the 2014 run
F 83753 conflated-series | the 2006 and 2014 runs both number from #1 here; LOCG's #1-22 sits on three different books and covers 18 issue files
F 83755 provider-disagrees | LOCG's #4-4 on Vol. 03, #9-9 on Vol. 07 and #10-10 on Vol. 08 are the volume ordinal echoed back from single-comic records, against GCD's #12-15 and the rest
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83748 now sits on S103897, whose file decides it
# u 83748 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83749 now sits on S103897, whose file decides it
# u 83749 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83750 now sits on S103897, whose file decides it
# u 83750 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83751 now sits on S103897, whose file decides it
# u 83751 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83752 now sits on S103897, whose file decides it
# u 83752 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83753 now sits on S103896, whose file decides it
# u 83753 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83754 now sits on S103896, whose file decides it
# u 83754 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83755 now sits on S103896, whose file decides it
# u 83755 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83756 now sits on S103896, whose file decides it
# u 83756 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83757 now sits on S103896, whose file decides it
# u 83757 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83758 now sits on S103896, whose file decides it
# u 83758 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83759 now sits on S103896, whose file decides it
# u 83759 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83760 now sits on S103896, whose file decides it
# u 83760 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83761 now sits on S103896, whose file decides it
# u 83761 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 83762 now sits on S103896, whose file decides it
# u 83762 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
u 117932 two runs number from #1 on this shelf and two collection lines share the volume ordinals; a range in either coordinate claims the other run's issues
