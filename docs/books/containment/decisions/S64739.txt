# G.I. Joe (S64739) — 21 collected editions across five IDW runs, and several are cover packs.
#
# IDW's G.I. Joe v1 (2008), v2 (2011), v3 (2013), the 2015 relaunch and v5 (2019) all sit here, together
# with Special Missions and the Cobra Command crossover volumes — so four different books are "Vol. 01"
# (33893 Homefront, 33894 Special Missions, 33901 The Fall of G.I. Joe, 33904 the 2017 line) and four are
# "Vol. 02". Items 32130, 32131 and 32132 are 1-5pp variant-cover packs, not collections; 32087-32110
# are 24-28pp v2 single issues.
# GCD's #1-5 on Homefront reaches five files that belong to other runs.



# the remaining 18 editions: five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues

N 64739 five IDW G.I. Joe runs plus Special Missions and Cobra Command share this Series id; four books are "Vol. 01" and several items flagged as collections are cover packs or floppies
F 33893 conflated-series | five IDW runs numbering from #1 under one Series id, with four different "Vol. 01"
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33893 now sits on S103462, whose file decides it
# u 33893 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33894 now sits on S103465, whose file decides it
# u 33894 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33896 now sits on S103465, whose file decides it
# u 33896 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33897 now sits on S103462, whose file decides it
# u 33897 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33899 now sits on S103465, whose file decides it
# u 33899 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33900 now sits on S103462, whose file decides it
# u 33900 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33901 now sits on S103463, whose file decides it
# u 33901 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33902 now sits on S103463, whose file decides it
# u 33902 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33904 now sits on S103464, whose file decides it
# u 33904 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 33905 now sits on S103464, whose file decides it
# u 33905 five IDW runs number from #1 on this shelf and the volume ordinals are reused across them; several items flagged as collections are 24-28pp single issues
