# Dust (S5947) — 1 volumes; 1 state their own chapters.
#
# Every page inside these archives is named with the chapter it belongs to, so each
# volume states its contents exactly. The ranges below are CHAPTER numbers read from
# the archives themselves, and they form a ladder: single volume.
# Volumes whose rip names no chapter are refused: a volume number is not a chapter
# number and must not stand in for one (PLAN.md §4.2).

N 5947 chapter ranges read from each archive's own page names; the ladder tiles (single volume)
# MOVED (series rebuild, retired by retire_moved_lines.py): item 43990 now sits on S104105, whose file decides it
# S 43990 1 8 0.97 | Dust 8 Omnibus | the archive names the chapter on every page and this volume holds 8 of them, c1-c8
