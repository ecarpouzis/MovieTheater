# The Mighty Thor (S19134) — 10 collected editions over THREE runs, 25 issue files.
#
# Three lines, three runs, one Series id:
#   Omnibus Books 01-04            reprint the 1966 Thor run
#   "by Matt Fraction" Vols. 01-02 collect The Mighty Thor v1 (2011), whose files (#5, #8-#22) are here
#   "Vol. NN" Vols. 01-04          collect The Mighty Thor v2 (2016), which numbers from #1 again
# So GCD's and ComicVine's #1-5, #6-12, #13-19 and #20-23 are the 2016 run's ranges, and every file they
# reach on this shelf belongs to the 2011 run instead — nine files under Vol. 03 alone.



# the remaining 10 editions: three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues

N 19134 three Mighty Thor runs (1966, 2011, 2016) share this Series id; the 2016 run's ranges land on the 2011 run's files, which are the ones the shelf actually holds
F 96710 conflated-series | The Mighty Thor 1966, 2011 and 2016 runs share this Series id; GCD's and ComicVine's #13-19 for the 2016 Vol. 03 covers nine 2011-run files
u 96685 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
u 96686 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96708 now sits on S103572, whose file decides it
# u 96708 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96709 now sits on S103572, whose file decides it
# u 96709 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96710 now sits on S103572, whose file decides it
# u 96710 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96711 now sits on S103572, whose file decides it
# u 96711 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96757 now sits on S103607, whose file decides it
# u 96757 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96758 now sits on S103607, whose file decides it
# u 96758 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96759 now sits on S103607, whose file decides it
# u 96759 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96760 now sits on S103607, whose file decides it
# u 96760 three runs share this shelf's numbering and the trades belong to different ones; a range would land on another run's issues
# MOVED (series rebuild, retired by retire_moved_lines.py): item 96707 now sits on S103572, whose file decides it
# u 96707 opened it; no collecting line. GCD's #700-706 is the legacy numbering, not this shelf's coordinate
