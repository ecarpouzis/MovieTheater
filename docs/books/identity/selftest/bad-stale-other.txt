# selftest: stale-flag fixture (TOOLS_TODO 37)
S 2 cv=113747 gcd=- 0.9 | CV volume and the GCD series agree on the 1998 start year and the run length, and our filenames number 1-12 with no gap
F 2 stale-flag=7 | every file on this shelf is one run: one folder, one ladder #1-12, one publisher and one start year; the flag's second run is a duplicate rip
F 2 stale-flag=8 | every file on this shelf is one run: one folder, one ladder #1-12, one publisher and one start year; the flag's second run is a duplicate rip
F 2 stale-flag=9 | every file on this shelf is one run: one folder, one ladder #1-12, one publisher and one start year; the flag's second run is a duplicate rip
F 2 stale-flag=10 | every file on this shelf is one run: one folder, one ladder #1-12, one publisher and one start year; the flag's second run is a duplicate rip
