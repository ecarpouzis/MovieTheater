# selftest: refused-partner fixture (TOOLS_TODO 31)
S 1527 cv=22340 gcd=- 0.9 | this shelf is the run that volume describes, and the two legs agree on start year and issue count, so the id belongs here
R 47160 | refused again in the same file, so the apply's frees arm clears its stored link
